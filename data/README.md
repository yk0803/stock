# Data directory

Raw and intermediate datasets are not included in this repository package. The notebook downloads index data from Yahoo Finance and constructs technical indicators. Prepared leakage-safe scenario files should be placed in `data/processed/` before rerunning the model-training phases.

The final paper result files that were supplied for the revision are stored under `results/` rather than `data/`.
